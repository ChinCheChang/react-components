import React from 'react';
import './iconBar.css';

const IconBar = () => {
  return(
    <div id="icon-bar" className="icon-bar grow-large">
      <i className="far fa-calendar-alt calendarIcon ba"></i>
    </div>
  );
}

export default IconBar;
